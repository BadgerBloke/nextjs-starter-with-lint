# Prettier formats YAML only — Biome owns everything else.
# Ignore-all, then allowlist *.yaml / *.yml at any depth.
*
!**/
!*.yaml
!*.yml
!**/*.yaml
!**/*.yml

# Hard-ignore vendor and build outputs.
node_modules/
.next/
dist/
build/
messages/
